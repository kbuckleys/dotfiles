// ┌─┐┌─┐┌┐┌┬ ┬┌─┐┬─┐┬┌─┌─┐
// ┌─┘├┤ │││││││ │├┬┘├┴┐└─┐
// └─┘└─┘┘└┘└┴┘└─┘┴└─┴ ┴└─┘
// https://github.com/kbuckleys/
// The one horizontal gap between two neighbouring bar modules.

import QtQuick
import "."

Item {
  implicitWidth: Zenon.gap
  implicitHeight: Zenon.slot
}
